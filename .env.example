# Used by docker-compose.yml when building/running the backend and frontend
# containers together (`docker compose up --build`). Copy to .env and fill
# in your Firebase web app config - see README for where to find these.

FIREBASE_PROJECT_ID=
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_APP_ID=
